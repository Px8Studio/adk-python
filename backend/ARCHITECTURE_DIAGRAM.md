# Orkhon Backend Architecture Diagram

> **Complete System Architecture for AI Agent Infrastructure**  
> Visual representation of all components, relationships, and data flows

---

## 🎨 High-Level Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           FRONTEND LAYER                                         │
│  ┌──────────────────────┐        ┌──────────────────────┐                      │
│  │   ADK Web UI         │        │  Jaeger Tracing UI   │                      │
│  │  (Angular SPA)       │        │   (Observability)    │                      │
│  │  Port: 4200          │        │   Port: 16686        │                      │
│  └──────────┬───────────┘        └──────────┬───────────┘                      │
└─────────────┼──────────────────────────────┼──────────────────────────────────┘
              │ HTTP/WebSocket               │ HTTP (Query Traces)
              │                              │
┌─────────────▼──────────────────────────────▼──────────────────────────────────┐
│                        AGENT ORCHESTRATION LAYER                               │
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────┐  │
│  │                     Google ADK (Agent Development Kit)                   │  │
│  │  ┌───────────────┐  ┌──────────────┐  ┌─────────────┐  ┌─────────────┐ │  │
│  │  │  LLM Agents   │  │    Runner    │  │   Session   │  │   Memory    │ │  │
│  │  │ - DNB Agent   │  │ (Reason-Act  │  │   Service   │  │   Service   │ │  │
│  │  │ - Custom      │  │    Loop)     │  │ (State Mgr) │  │ (Long-term) │ │  │
│  │  │   Agents      │  │              │  │             │  │             │ │  │
│  │  └───────┬───────┘  └──────┬───────┘  └─────────────┘  └─────────────┘ │  │
│  │          │                 │                                            │  │
│  │  ┌───────▼─────────────────▼──────────────────────────────────────────┐│  │
│  │  │                    ADK Agent Implementations                        ││  │
│  │  │  • agents/dnb_agent/agent.py (root_agent)                          ││  │
│  │  │  • simple_dnb_agent.py (LangGraph + ToolboxClient)                 ││  │
│  │  │  • simple_agent.ipynb (Interactive development)                    ││  │
│  │  └─────────────────────────────────────────────────────────────────────┘│  │
│  │                                                                           │  │
│  │  ┌─────────────────────────────────────────────────────────────────────┐│  │
│  │  │                     LLM Integration Layer                            ││  │
│  │  │  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐  ││  │
│  │  │  │ Google Gemini    │  │  Vertex AI       │  │  LangChain       │  ││  │
│  │  │  │ - 2.5-flash      │  │  Integration     │  │  - ChatVertexAI  │  ││  │
│  │  │  │ - 2.0-flash      │  │  - Project Init  │  │  - ReAct Agent   │  ││  │
│  │  │  └──────────────────┘  └──────────────────┘  └──────────────────┘  ││  │
│  │  └─────────────────────────────────────────────────────────────────────┘│  │
│  └───────────────────────────────────────┬─────────────────────────────────┘  │
└──────────────────────────────────────────┼────────────────────────────────────┘
                                           │
                                           │ ToolboxClient / ToolboxToolset
                                           │ HTTP REST API (Port 5000)
                                           │
┌──────────────────────────────────────────▼────────────────────────────────────┐
│                       TOOL ORCHESTRATION LAYER                                 │
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────┐  │
│  │                  GenAI Toolbox MCP Server (Docker)                       │  │
│  │                       Port: 5000 | Service: genai-toolbox-mcp            │  │
│  │  ┌───────────────────────────────────────────────────────────────────┐  │  │
│  │  │                    Core Toolbox Services                           │  │  │
│  │  │  • Tool Registry & Discovery (/api/toolset, /api/tools)          │  │  │
│  │  │  • HTTP Tool Executor (invoke tools)                              │  │  │
│  │  │  • Schema Validation & Type Mapping                               │  │  │
│  │  │  • Authentication Management (DNB API keys)                       │  │  │
│  │  │  • Connection Pooling & Request Management                        │  │  │
│  │  │  • MCP Protocol Handler (JSON-RPC 2.0)                            │  │  │
│  │  └───────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                           │  │
│  │  ┌───────────────────────────────────────────────────────────────────┐  │  │
│  │  │                    Tool Configuration Layer                        │  │  │
│  │  │  📁 /config/dev/ (Development Tools)                              │  │  │
│  │  │     • dnb-echo-tools.yaml (3 tools)                               │  │  │
│  │  │     • dnb-statistics-tools.yaml (79 tools)                        │  │  │
│  │  │     • dnb-public-register-tools.yaml (5 tools)                    │  │  │
│  │  │                                                                    │  │  │
│  │  │  📁 /config/prod/ (Production Tools)                              │  │  │
│  │  │     • dnb-*.yaml (same structure, prod endpoints)                 │  │  │
│  │  └───────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                           │  │
│  │  ┌───────────────────────────────────────────────────────────────────┐  │  │
│  │  │                  OpenTelemetry Integration                         │  │  │
│  │  │  • Span Creation (tool invocations)                               │  │  │
│  │  │  • Trace Context Propagation                                      │  │  │
│  │  │  • OTLP Exporter → jaeger:4318                                    │  │  │
│  │  │  • Service Name: ${OTEL_SERVICE_NAME}                             │  │  │
│  │  └───────────────────────────────────────────────────────────────────┘  │  │
│  └─────────────────────────┬──────────────────┬────────────────────────────┘  │
└────────────────────────────┼──────────────────┼───────────────────────────────┘
                             │                  │
                             │ HTTP/REST        │ OTLP (Port 4318)
                             │                  │
            ┌────────────────▼───────┐    ┌─────▼──────────────────────────┐
            │   DNB External APIs    │    │  Jaeger All-in-One Container   │
            │   (api.dnb.nl)         │    │  (Tracing Backend)             │
            └────────────────────────┘    └────────────────────────────────┘
                             │                  │
┌────────────────────────────▼──────────────────▼───────────────────────────────┐
│                       EXTERNAL SERVICES LAYER                                  │
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────┐  │
│  │                          DNB API Services                                │  │
│  │  ┌──────────────────────┐  ┌──────────────────────┐  ┌──────────────┐  │  │
│  │  │  Statistics API      │  │  Public Register API │  │   Echo API   │  │  │
│  │  │  v2024100101         │  │  v1                  │  │  (Testing)   │  │  │
│  │  │  79 endpoints        │  │  5 endpoints         │  │  3 endpoints │  │  │
│  │  │  • Metadata          │  │  • Entity data       │  │  • Health    │  │  │
│  │  │  • Data queries      │  │  • Search            │  │  • Echo      │  │  │
│  │  │  • Transformations   │  │  • Filters           │  │  • Validate  │  │  │
│  │  └──────────────────────┘  └──────────────────────┘  └──────────────┘  │  │
│  │                                                                           │  │
│  │  🔑 Authentication: Ocp-Apim-Subscription-Key                            │  │
│  │  📍 Base URL: https://api.dnb.nl                                         │  │
│  └─────────────────────────────────────────────────────────────────────────┘  │
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────┐  │
│  │                      Observability Services                              │  │
│  │  ┌──────────────────────────────────────────────────────────────────┐   │  │
│  │  │  Jaeger Tracing (all-in-one:latest)                              │   │  │
│  │  │  • Collector (OTLP) - Port 4318                                  │   │  │
│  │  │  • Query Service - Port 16686                                    │   │  │
│  │  │  • Storage: In-Memory (Dev) / Badger (Persistent option)         │   │  │
│  │  │  • Health Check: Port 14269                                      │   │  │
│  │  └──────────────────────────────────────────────────────────────────┘   │  │
│  └─────────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                      DEVELOPMENT & BUILD LAYER                                  │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                      AgentBox (OpenAPI Converter)                        │   │
│  │  ┌───────────────────────────────────────────────────────────────────┐  │   │
│  │  │  openapi_to_toolbox.py - OpenAPI → Toolbox YAML Converter        │  │   │
│  │  │                                                                    │  │   │
│  │  │  Input:  📄 apis/dnb/specs/*.yaml (OpenAPI 3.0 specs)           │  │   │
│  │  │          • openapi3_statisticsdatav2024100101.yaml                │  │   │
│  │  │          • openapi3_publicdatav1.yaml                             │  │   │
│  │  │          • openapi3_echo.yaml                                     │  │   │
│  │  │                                                                    │  │   │
│  │  │  Process: • Path-based tool naming                                │  │   │
│  │  │           • Parameter extraction & type mapping                   │  │   │
│  │  │           • Request body template generation                      │  │   │
│  │  │           • Environment-specific configs (dev/prod)               │  │   │
│  │  │           • Schema validation                                     │  │   │
│  │  │                                                                    │  │   │
│  │  │  Output: 📦 Generated YAML Tool Definitions                       │  │   │
│  │  │          • apis/dnb/generated/tools.*.generated.yaml              │  │   │
│  │  │          • toolbox/config/dev/*.yaml (deployed)                   │  │   │
│  │  │          • toolbox/config/prod/*.yaml (deployed)                  │  │   │
│  │  └───────────────────────────────────────────────────────────────────┘  │   │
│  │                                                                           │   │
│  │  ⚙️ CLI Commands:                                                        │   │
│  │     • python openapi_to_toolbox.py convert --all                        │   │
│  │     • python openapi_to_toolbox.py validate --api echo                  │   │
│  │     • python openapi_to_toolbox.py diff --api statistics                │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                      Scripts & Automation                                │   │
│  │  • backend\scripts\start-adk-web.ps1 (PowerShell launcher)             │   │
│  │  • docker-compose.dev.yml (Dev environment)                             │   │
│  │  • docker-compose.prod.yml (Prod environment)                           │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow & Component Interactions

### 1️⃣ Agent Query Flow (End-to-End)

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ Step 1: User Interaction                                                     │
└──────────────────────────────────────────────────────────────────────────────┘
   User Query: "Get the latest exchange rates of the euro and gold price"
           │
           ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│ Step 2: ADK Agent (DNB Agent)                                                │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ simple_dnb_agent.py / agents/dnb_agent/agent.py                        │ │
│  │  • Receives user query                                                 │ │
│  │  • LangGraph ReAct loop initiates                                      │ │
│  │  • Gemini LLM analyzes intent                                          │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────┬─────────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│ Step 3: Tool Discovery & Selection                                           │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ ToolboxClient.aload_toolset()                                          │ │
│  │  • HTTP GET → http://localhost:5000/api/toolset/dnb-tools              │ │
│  │  • Receives list of 84 available DNB tools                             │ │
│  │  • Gemini selects relevant tools:                                      │ │
│  │    - dnb-statistics-get-series-observations (exchange rates)           │ │
│  │    - dnb-statistics-search-metadata (gold price series)                │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────┬─────────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│ Step 4: Tool Invocation (via GenAI Toolbox)                                  │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ HTTP POST → http://localhost:5000/api/tool/[tool-name]/invoke         │ │
│  │                                                                         │ │
│  │ Request Body:                                                           │ │
│  │ {                                                                       │ │
│  │   "seriesKey": "EUR_USD_EXCHANGE_RATE",                                │ │
│  │   "startDate": "2024-01-01",                                           │ │
│  │   "endDate": "2024-10-17"                                              │ │
│  │ }                                                                       │ │
│  │                                                                         │ │
│  │ GenAI Toolbox:                                                         │ │
│  │  1. Validates request against tool schema                              │ │
│  │  2. Creates OpenTelemetry span                                         │ │
│  │  3. Injects authentication (DNB_SUBSCRIPTION_KEY_DEV)                  │ │
│  │  4. Makes HTTP request to DNB API                                      │ │
│  │  5. Exports trace to Jaeger                                            │ │
│  │  6. Returns structured response                                        │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────┬─────────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│ Step 5: External API Call (DNB Statistics API)                               │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ HTTPS GET → https://api.dnb.nl/statistics/v2024100101/data             │ │
│  │                                                                         │ │
│  │ Headers:                                                                │ │
│  │   Ocp-Apim-Subscription-Key: ${DNB_SUBSCRIPTION_KEY_DEV}               │ │
│  │   Accept: application/json                                             │ │
│  │                                                                         │ │
│  │ Query Params: ?seriesKey=EUR_USD_EXCHANGE_RATE&startDate=...          │ │
│  │                                                                         │ │
│  │ Response: JSON data with exchange rate observations                    │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────┬─────────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│ Step 6: Response Processing & Reasoning                                      │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ ADK Agent (Gemini LLM)                                                 │ │
│  │  • Receives structured tool response                                   │ │
│  │  • Analyzes data (exchange rates)                                      │ │
│  │  • Determines if more tools needed (gold price)                        │ │
│  │  • Iterates ReAct loop if necessary                                    │ │
│  │  • Generates natural language response                                 │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────┬─────────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│ Step 7: User Response                                                         │
│  "Based on the latest DNB data, the EUR/USD exchange rate is 1.08 and       │
│   the gold price is €1,850 per ounce as of October 17, 2024."               │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

### 2️⃣ Development Workflow (OpenAPI → Deployed Tools)

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ Phase 1: OpenAPI Spec Update                                                 │
└──────────────────────────────────────────────────────────────────────────────┘
   Developer updates: apis/dnb/specs/openapi3_statisticsdatav2024100101.yaml
           │
           ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│ Phase 2: Tool Generation (AgentBox)                                          │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ $ cd backend/agentbox                                                  │ │
│  │ $ python openapi_to_toolbox.py convert --all                           │ │
│  │                                                                         │ │
│  │ Process for each API:                                                  │ │
│  │  1. Parse OpenAPI 3.0 spec                                             │ │
│  │  2. Extract endpoints, parameters, schemas                             │ │
│  │  3. Generate tool names from paths:                                    │ │
│  │     GET /statistics/metadata → dnb-statistics-get-metadata             │ │
│  │  4. Map OpenAPI types to GenAI Toolbox types:                          │ │
│  │     • string → string                                                  │ │
│  │     • integer → integer                                                │ │
│  │     • object → json                                                    │ │
│  │  5. Create request body templates                                      │ │
│  │  6. Add authentication config (DNB API key)                            │ │
│  │  7. Generate YAML output                                               │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────┬─────────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│ Phase 3: Generated Tool Files                                                │
│  📁 apis/dnb/generated/                                                      │
│     • tools.statistics.generated.yaml (79 tools)                            │
│     • tools.public-register.generated.yaml (5 tools)                        │
│     • tools.echo.generated.yaml (3 tools)                                   │
│                                                                              │
│  Auto-copied to:                                                             │
│  📁 toolbox/config/dev/                                                      │
│     • dnb-statistics-tools.yaml                                             │
│     • dnb-public-register-tools.yaml                                        │
│     • dnb-echo-tools.yaml                                                   │
└────────────────────────────────────┬─────────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│ Phase 4: Toolbox Restart & Registration                                      │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ $ cd backend/toolbox                                                   │ │
│  │ $ docker-compose -f docker-compose.dev.yml restart genai-toolbox-mcp  │ │
│  │                                                                         │ │
│  │ GenAI Toolbox startup:                                                 │ │
│  │  1. Reads all YAML files from /config/dev/                             │ │
│  │  2. Parses tool definitions                                            │ │
│  │  3. Validates schemas                                                  │ │
│  │  4. Registers 84 tools in internal registry                            │ │
│  │  5. Creates toolsets (dnb-tools, dnb-statistics, etc.)                 │ │
│  │  6. Exposes REST API on port 5000                                      │ │
│  │  7. Logs: "Server ready to serve!"                                     │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────┬─────────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│ Phase 5: Testing & Verification                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ UI: http://localhost:5000/ui/                                          │ │
│  │  • Browse all tools                                                    │ │
│  │  • Test individual tool invocations                                    │ │
│  │  • View request/response schemas                                       │ │
│  │                                                                         │ │
│  │ API: http://localhost:5000/api/toolset                                 │ │
│  │  • GET /api/toolset → List all toolsets                                │ │
│  │  • GET /api/toolset/dnb-tools → List DNB tools                         │ │
│  │  • POST /api/tool/{name}/invoke → Invoke tool                          │ │
│  │                                                                         │ │
│  │ Jaeger: http://localhost:16686                                         │ │
│  │  • View distributed traces                                             │ │
│  │  • Analyze tool performance                                            │ │
│  │  • Debug failures                                                      │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 🧩 Component Relationships & Dependencies

### Component Dependency Graph

```
┌───────────────────────────────────────────────────────────────────────────┐
│                         Component Dependencies                             │
└───────────────────────────────────────────────────────────────────────────┘

ADK Web UI (Angular)
  ├─ Depends on → ADK API Server (Python/FastAPI)
  │               └─ Depends on → ADK Runner
  │                               └─ Depends on → ADK Agents
  │                                               ├─ LLM (Gemini/Vertex AI)
  │                                               └─ ToolboxToolset
  └─ Optional → Jaeger UI (read-only)

Simple DNB Agent (LangGraph)
  ├─ Depends on → LangChain (ChatVertexAI, create_react_agent)
  ├─ Depends on → ToolboxClient (toolbox-langchain)
  │               └─ HTTP Client → GenAI Toolbox MCP Server
  └─ Depends on → Vertex AI (vertexai.init)

ADK Agent (agents/dnb_agent/agent.py)
  ├─ Depends on → google.adk.Agent
  ├─ Depends on → ToolboxToolset
  │               └─ HTTP Client → GenAI Toolbox MCP Server
  └─ Depends on → Gemini LLM (via ADK Runner)

GenAI Toolbox MCP Server (Docker)
  ├─ Depends on → Tool YAML Configurations (/config/dev/*.yaml)
  ├─ Depends on → Jaeger (OpenTelemetry OTLP endpoint)
  ├─ Depends on → DNB API Services (external HTTP)
  └─ Depends on → Environment Variables (API keys)

AgentBox (openapi_to_toolbox.py)
  ├─ Input → OpenAPI 3.0 Specs (apis/dnb/specs/*.yaml)
  └─ Output → GenAI Toolbox YAML (toolbox/config/dev/*.yaml)

Jaeger Tracing
  ├─ Receives from → GenAI Toolbox (OTLP/gRPC port 4318)
  └─ Storage → In-Memory (dev) / Badger DB (prod option)

Docker Compose Dev
  ├─ Orchestrates → GenAI Toolbox Container
  ├─ Orchestrates → Jaeger Container
  └─ Network → orkhon-network (shared)
```

---

### Integration Points & Protocols

```
┌───────────────────────────────────────────────────────────────────────────┐
│                      Integration Points Matrix                            │
└───────────────────────────────────────────────────────────────────────────┘

┌─────────────────┬─────────────────┬──────────────┬────────────────────┐
│ From Component  │ To Component    │ Protocol     │ Port/Endpoint      │
├─────────────────┼─────────────────┼──────────────┼────────────────────┤
│ ADK Web UI      │ ADK API Server  │ HTTP/WS      │ 4200 → Backend API │
│ ADK Agent       │ GenAI Toolbox   │ HTTP/REST    │ → 5000/api/        │
│ LangGraph Agent │ GenAI Toolbox   │ HTTP/REST    │ → 5000/api/        │
│ GenAI Toolbox   │ DNB APIs        │ HTTPS/REST   │ → api.dnb.nl       │
│ GenAI Toolbox   │ Jaeger          │ OTLP/gRPC    │ → 4318/v1/traces   │
│ User Browser    │ Jaeger UI       │ HTTP         │ → 16686            │
│ User Browser    │ Toolbox UI      │ HTTP         │ → 5000/ui/         │
│ AgentBox        │ File System     │ File I/O     │ Local YAML files   │
└─────────────────┴─────────────────┴──────────────┴────────────────────┘
```

---

## 📦 Technology Stack by Layer

### Frontend Layer
```
┌─────────────────────────────────────────────────────────────────┐
│ Technology                  │ Purpose                          │
├─────────────────────────────┼──────────────────────────────────┤
│ Angular 18+                 │ ADK Web UI framework             │
│ TypeScript                  │ Type-safe frontend code          │
│ RxJS                        │ Reactive programming             │
│ Angular Material            │ UI components                    │
│ WebSocket                   │ Real-time agent communication    │
└─────────────────────────────┴──────────────────────────────────┘
```

### Agent Orchestration Layer
```
┌─────────────────────────────────────────────────────────────────┐
│ Technology                  │ Purpose                          │
├─────────────────────────────┼──────────────────────────────────┤
│ Google ADK (Python)         │ Agent framework                  │
│ LangGraph                   │ Agent workflow orchestration     │
│ LangChain                   │ LLM abstractions & ReAct pattern │
│ Google Gemini 2.5/2.0       │ Large Language Model             │
│ Vertex AI                   │ Google Cloud AI platform         │
│ toolbox-langchain           │ Toolbox client for LangChain     │
│ toolbox-core (Python)       │ ADK ToolboxToolset integration   │
│ FastAPI                     │ ADK API server                   │
└─────────────────────────────┴──────────────────────────────────┘
```

### Tool Orchestration Layer
```
┌─────────────────────────────────────────────────────────────────┐
│ Technology                  │ Purpose                          │
├─────────────────────────────┼──────────────────────────────────┤
│ GenAI Toolbox (Go)          │ MCP server implementation        │
│ Docker                      │ Container runtime                │
│ Docker Compose              │ Multi-container orchestration    │
│ OpenTelemetry               │ Distributed tracing SDK          │
│ OTLP                        │ Telemetry protocol               │
│ YAML                        │ Tool configuration format        │
│ JSON-RPC 2.0                │ MCP protocol                     │
└─────────────────────────────┴──────────────────────────────────┘
```

### Development & Build Layer
```
┌─────────────────────────────────────────────────────────────────┐
│ Technology                  │ Purpose                          │
├─────────────────────────────┼──────────────────────────────────┤
│ Python 3.11+                │ Development language             │
│ uv                          │ Fast Python package manager      │
│ Poetry                      │ Dependency management (ADK)      │
│ OpenAPI 3.0                 │ API specification format         │
│ PyYAML                      │ YAML parsing/generation          │
│ PowerShell                  │ Windows automation scripts       │
└─────────────────────────────┴──────────────────────────────────┘
```

### Observability Layer
```
┌─────────────────────────────────────────────────────────────────┐
│ Technology                  │ Purpose                          │
├─────────────────────────────┼──────────────────────────────────┤
│ Jaeger                      │ Distributed tracing backend      │
│ OpenTelemetry               │ Observability framework          │
│ Badger DB (optional)        │ Persistent trace storage         │
│ Prometheus (future)         │ Metrics collection               │
└─────────────────────────────┴──────────────────────────────────┘
```

---

## 🔐 Security & Authentication Flow

```
┌───────────────────────────────────────────────────────────────────────────┐
│                       Authentication Architecture                          │
└───────────────────────────────────────────────────────────────────────────┘

Environment Variables (.env)
  ├─ DNB_SUBSCRIPTION_KEY_DEV=<key>
  ├─ DNB_SUBSCRIPTION_KEY_PROD=<key>
  ├─ DNB_SUBSCRIPTION_NAME_DEV=<subscription-name>
  └─ DNB_SUBSCRIPTION_NAME_PROD=<subscription-name>
           │
           ▼
Docker Compose (docker-compose.dev.yml)
  └─ env_file: .env
           │
           ▼
GenAI Toolbox Container
  └─ Environment: DNB_SUBSCRIPTION_KEY_DEV
           │
           ▼
Tool YAML Configuration (dnb-*-tools.yaml)
  ├─ authentication:
  │    name: dnb-dev-auth
  │    kind: http
  │    scheme: apiKey
  │    in: header
  │    key: Ocp-Apim-Subscription-Key
  │    value: ${DNB_SUBSCRIPTION_KEY_DEV}
  └─ tools:
       └─ authentication: dnb-dev-auth
           │
           ▼
HTTP Request to DNB API
  └─ Headers:
       Ocp-Apim-Subscription-Key: <actual-key-from-env>
```

---

## 🗄️ Data Models & Schemas

### ADK Session Schema
```yaml
Session:
  id: string (UUID)
  agent_id: string
  created_at: datetime
  events: Event[]
    - id: string
      type: USER_MESSAGE | AGENT_MESSAGE | TOOL_CALL | TOOL_RESULT
      timestamp: datetime
      content: string | object
  state: dict (custom state)
```

### GenAI Toolbox Tool Schema
```yaml
Tool:
  name: string (dnb-statistics-get-metadata)
  description: string
  inputSchema:
    type: object
    properties:
      param_name:
        type: string | integer | boolean | json
        description: string
        required: boolean
  authentication: string (ref to auth config)
  source: string (http_source_dnb_dev)
  endpoint: string (/statistics/v2024100101/metadata)
  method: GET | POST | PUT | DELETE
```

### OpenTelemetry Span Schema
```json
{
  "traceId": "hex-string",
  "spanId": "hex-string",
  "parentSpanId": "hex-string",
  "name": "dnb-statistics-get-metadata",
  "startTime": "2024-10-17T10:00:00Z",
  "endTime": "2024-10-17T10:00:01Z",
  "attributes": {
    "http.method": "GET",
    "http.url": "https://api.dnb.nl/statistics/.../metadata",
    "http.status_code": 200,
    "tool.name": "dnb-statistics-get-metadata"
  }
}
```

---

## 🚀 Deployment Architecture

### Development Environment
```
Local Machine (Windows)
  ├─ Docker Desktop
  │    ├─ GenAI Toolbox Container (Port 5000)
  │    └─ Jaeger Container (Ports 16686, 4318)
  ├─ Python Virtual Environment (.venv)
  │    ├─ ADK Python SDK
  │    ├─ LangChain + LangGraph
  │    └─ Agent implementations
  └─ Node.js (Optional)
       └─ ADK Web UI (ng serve → Port 4200)
```

### Production Architecture (Conceptual)
```
Google Cloud Platform
  ├─ Cloud Run
  │    ├─ GenAI Toolbox Service (Auto-scaling)
  │    └─ ADK API Server (FastAPI)
  ├─ Vertex AI
  │    └─ Gemini LLM (Managed service)
  ├─ Cloud Trace (Jaeger replacement)
  ├─ Secret Manager
  │    └─ DNB API Keys
  └─ Cloud Storage
       └─ Session persistence
```

---

## 📊 Performance & Scalability

### Current Metrics (Development)
```
┌──────────────────────────────────────────────────────────────┐
│ Metric                     │ Value                          │
├────────────────────────────┼────────────────────────────────┤
│ Total Tools Available      │ 84 (DNB APIs)                  │
│ Tool Registration Time     │ < 2 seconds                    │
│ Average Tool Response Time │ 200-500ms (DNB API dependent)  │
│ Concurrent Tool Calls      │ Limited by Docker resources    │
│ Trace Retention            │ In-memory (until restart)      │
└────────────────────────────┴────────────────────────────────┘
```

### Bottlenecks & Optimization Opportunities
```
1. GenAI Toolbox
   - Single container (scale horizontally with load balancer)
   - In-memory caching (add Redis for shared cache)

2. DNB APIs
   - External rate limits (implement retry with backoff)
   - Response times vary (add response caching)

3. Gemini LLM
   - Cloud service (managed by Google)
   - Consider token usage optimization

4. Jaeger
   - In-memory storage (switch to Cassandra/Elasticsearch for prod)
```

---

## 🎯 Key Architectural Decisions

### Why GenAI Toolbox?
- ✅ **Declarative Tool Configuration**: YAML-based, no code changes
- ✅ **Built-in Authentication**: Handles API key management
- ✅ **OpenTelemetry Integration**: Native distributed tracing
- ✅ **MCP Protocol**: Standard for AI tool interoperability
- ✅ **Docker-based**: Easy deployment and scaling

### Why ADK (Agent Development Kit)?
- ✅ **Google Ecosystem**: Optimized for Gemini and Vertex AI
- ✅ **Code-First**: Python-native, testable, versionable
- ✅ **Multi-Agent Support**: Sequential, parallel, loop patterns
- ✅ **Session Management**: Built-in state persistence
- ✅ **Web UI**: Out-of-the-box agent testing interface

### Why LangGraph?
- ✅ **Workflow Orchestration**: Complex agent reasoning patterns
- ✅ **ReAct Pattern**: Built-in Reason-Act loops
- ✅ **State Management**: Explicit state transitions
- ✅ **Tool Integration**: First-class tool support

### Why OpenAPI → Toolbox Converter?
- ✅ **DRY Principle**: Single source of truth (OpenAPI spec)
- ✅ **Automation**: 84 tools from 3 specs, no manual YAML
- ✅ **Consistency**: Standardized tool naming and schemas
- ✅ **Maintainability**: Update spec, regenerate tools

---

## 🔮 Future Architecture Enhancements

```
┌───────────────────────────────────────────────────────────────┐
│ Planned Enhancements                                          │
├───────────────────────────────────────────────────────────────┤
│ 1. Multi-Agent Orchestration                                 │
│    • SequentialAgent for multi-step workflows                │
│    • ParallelAgent for concurrent DNB API calls              │
│    • LoopAgent for iterative data refinement                 │
│                                                               │
│ 2. Advanced Observability                                    │
│    • Prometheus metrics (tool usage, latency)                │
│    • Grafana dashboards (real-time monitoring)               │
│    • Custom metrics (cost tracking, token usage)             │
│                                                               │
│ 3. Persistent Storage                                        │
│    • PostgreSQL for session/memory persistence               │
│    • Elasticsearch for trace storage                         │
│    • Redis for tool response caching                         │
│                                                               │
│ 4. Enhanced Security                                         │
│    • OAuth 2.0 for user authentication                       │
│    • Role-based access control (RBAC)                        │
│    • Audit logging for compliance                            │
│                                                               │
│ 5. Production Deployment                                     │
│    • Kubernetes for container orchestration                  │
│    • Horizontal pod autoscaling (HPA)                        │
│    • Multi-region deployment                                 │
└───────────────────────────────────────────────────────────────┘
```

---

## 📚 Documentation References

- **ADK Documentation**: See `adk-python/contributing/adk_project_overview_and_architecture.md`
- **GenAI Toolbox Docs**: See `genai-toolbox/README.md` and `genai-toolbox/docs/`
- **Component READMEs**:
  - `backend/adk/README.md` - Agent development guide
  - `backend/agentbox/README.md` - OpenAPI conversion guide
  - `backend/apis/dnb/DNB API Services.MD` - DNB API integration
  - `backend/toolbox/config/QUICK_ANSWER.md` - Tool configuration guide

---

**Last Updated**: October 17, 2024  
**Architecture Version**: 1.0  
**Maintained By**: Orkhon Development Team
