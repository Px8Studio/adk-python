sources:
- id: statistics-api
  type: http
  config:
    base_url: https://api.dnb.nl/statisticsdata/v2024100101
    headers:
      Ocp-Apim-Subscription-Key: ${DNB_SUBSCRIPTION_KEY_PROD}
    timeout: 30
tools:
- id: getexchangeratesoftheeuroandgoldpriceday
  source_id: statistics-api
  method: GET
  path: /exchange-rates-of-the-euro-and-gold-price-day
  description: Exchange rates of the euro and gold price (Day)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getmarketinterestratesday
  source_id: statistics-api
  method: GET
  path: /market-interest-rates-day
  description: Market interest rates (Day)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getmarketinterestratesmonth
  source_id: statistics-api
  method: GET
  path: /market-interest-rates-month
  description: Market interest rates (Month)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getthe25largestinvestmentsinindividuallistedshares
  source_id: statistics-api
  method: GET
  path: /the-25-largest-investments-in-individual-listed-shares
  description: The ‘25 largest’ investments in individual listed shares
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getdutchhouseholdsavingsmonth
  source_id: statistics-api
  method: GET
  path: /dutch-household-savings-month
  description: Dutch household savings (Month)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getmfihouseholddepositsandloansinterestratesmonth
  source_id: statistics-api
  method: GET
  path: /mfi-household-deposits-and-loans-interest-rates-month
  description: MFI household deposits and loans, interest rates (Month)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getstatutoryinterestratehalfyear
  source_id: statistics-api
  method: GET
  path: /statutory-interest-rate-half-year
  description: Statutory interest rate (Half-year)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getexchangeratesoftheeuroandgoldpricemonth
  source_id: statistics-api
  method: GET
  path: /exchange-rates-of-the-euro-and-gold-price-month
  description: Exchange rates of the euro and gold price (Month)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getbalancesheetofdenederlandschebankmonthnonbreakadjusted
  source_id: statistics-api
  method: GET
  path: /balance-sheet-of-de-nederlandsche-bank-month-non-break-adjusted
  description: Balance sheet of De Nederlandsche Bank (Month, non-break adjusted)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getindividualpensionfunddataquarter
  source_id: statistics-api
  method: GET
  path: /individual-pension-fund-data-quarter
  description: Individual pension fund data (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getkeyindicatorsmonetarystatisticsmonth
  source_id: statistics-api
  method: GET
  path: /key-indicators-monetary-statistics-month
  description: Key indicators monetary statistics (Month)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getholdingofbondsbytypeofsustainabilitycharacteristicbysector
  source_id: statistics-api
  method: GET
  path: /holding-of-bonds-by-type-of-sustainability-characteristic-by-sector
  description: Holding of bonds by type of sustainability characteristic by sector
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getholdingofgreenbondswithexternalassurancebysector
  source_id: statistics-api
  method: GET
  path: /holding-of-green-bonds-with-external-assurance-by-sector
  description: 'Holding of green bonds with external assurance by sector '
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getissuanceofbondsbytypeofsustainabilitycharacteristicbysector
  source_id: statistics-api
  method: GET
  path: /issuance-of-bonds-by-type-of-sustainability-characteristic-by-sector
  description: Issuance of bonds by type of sustainability characteristic by sector
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getissuanceofgreenbondswithexternalassurancebysector
  source_id: statistics-api
  method: GET
  path: /issuance-of-green-bonds-with-external-assurance-by-sector
  description: Issuance of green bonds with external assurance by sector
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getinvestmentsbydutchhouseholdsinsecuritiesbyinstrumentcategory
  source_id: statistics-api
  method: GET
  path: /investments-by-dutch-households-in-securities-by-instrument-category
  description: Investments by Dutch households in securities by instrument category
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getinvestmentsbydutchhouseholdsinindividualaexshares
  source_id: statistics-api
  method: GET
  path: /investments-by-dutch-households-in-individual-aex-shares
  description: Investments by Dutch households in individual AEX shares
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getthe25largestinvestmentsbydutchhouseholdsinindividualinvestmentfunds
  source_id: statistics-api
  method: GET
  path: /the-25-largest-investments-by-dutch-households-in-individual-investment-funds
  description: The ‘25 largest’ investments by Dutch households in individual investment
    funds
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getexchangeratesoftheeuroandgoldpricequarter
  source_id: statistics-api
  method: GET
  path: /exchange-rates-of-the-euro-and-gold-price-quarter
  description: Exchange rates of the euro and gold price (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getexchangeratesoftheeuroandgoldpriceyearendofperiod
  source_id: statistics-api
  method: GET
  path: /exchange-rates-of-the-euro-and-gold-price-year-end-of-period
  description: Exchange rates of the euro and gold price (Year, end-of-period)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getexchangeratesoftheeuroandgoldpriceyearaverage
  source_id: statistics-api
  method: GET
  path: /exchange-rates-of-the-euro-and-gold-price-year-average
  description: Exchange rates of the euro and gold price (Year, average)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getpremiumsclaimsandcostsbylineofbusinessofnonlifeinsurerssolvencyiiyear
  source_id: statistics-api
  method: GET
  path: /premiums-claims-and-costs-by-line-of-business-of-non-life-insurers-solvency-ii-year
  description: Premiums, claims and costs by line of business of non-life insurers,
    Solvency II (Year)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getpremiumsclaimsandcostsbylineofbusinessoflifeinsurerssolvencyiiyear
  source_id: statistics-api
  method: GET
  path: /premiums-claims-and-costs-by-line-of-business-of-life-insurers-solvency-ii-year
  description: Premiums, claims and costs by line of business of life insurers, Solvency
    II (Year)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getpremiumsclaimsandcostsbylineofbusinessoflifeinsurerssolvencyiiquarter
  source_id: statistics-api
  method: GET
  path: /premiums-claims-and-costs-by-line-of-business-of-life-insurers-solvency-ii-quarter
  description: Premiums, claims and costs by line of business of life insurers, Solvency
    II (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getpremiumsclaimsandcostsbylineofbusinessofnonlifeinsurerssolvencyiiquarter
  source_id: statistics-api
  method: GET
  path: /premiums-claims-and-costs-by-line-of-business-of-non-life-insurers-solvency-ii-quarter
  description: Premiums, claims and costs by line of business of non-life insurers,
    Solvency II (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: geteuropeancentralbankinterestrates
  source_id: statistics-api
  method: GET
  path: /european-central-bank-interest-rates
  description: European Central Bank interest rates
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getfinancialdataofindividualbankshalfyear
  source_id: statistics-api
  method: GET
  path: /financial-data-of-individual-banks-half-year
  description: Financial data of individual banks (Half-year)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getnominalirtsforpensionfundszerocoupon
  source_id: statistics-api
  method: GET
  path: /nominal-irts-for-pension-funds-zero-coupon
  description: Nominal IRTS for pension funds (zero coupon)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getbalancesheetofdutchbasedmfisnotincludingdnbmonth
  source_id: statistics-api
  method: GET
  path: /balance-sheet-of-dutch-based-mfis-not-including-dnb-month
  description: Balance sheet of Dutch-based MFIs (not including DNB) (Month)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getsecuritiesandderivativestradersbalancesheet
  source_id: statistics-api
  method: GET
  path: /securities-and-derivatives-traders-balance-sheet
  description: Securities and Derivatives Traders balance sheet
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getfinancecompaniesbalancesheet
  source_id: statistics-api
  method: GET
  path: /finance-companies-balance-sheet
  description: Finance Companies balance sheet
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getspecialisedfinancialinstitutionsbalancesheet
  source_id: statistics-api
  method: GET
  path: /specialised-financial-institutions-balance-sheet
  description: Specialised Financial Institutions balance sheet
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getfinancialauxiliariesbalancesheet
  source_id: statistics-api
  method: GET
  path: /financial-auxiliaries-balance-sheet
  description: Financial Auxiliaries balance sheet
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getheadofficesoffinancialauxiliariesbalancesheet
  source_id: statistics-api
  method: GET
  path: /head-offices-of-financial-auxiliaries-balance-sheet
  description: Head Offices of Financial Auxiliaries balance sheet
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getotherfinancialauxiliariesbalancesheet
  source_id: statistics-api
  method: GET
  path: /other-financial-auxiliaries-balance-sheet
  description: Other Financial Auxiliaries balance sheet
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getbalancesheetofotherfinancialintermediariesbygeographyquarter
  source_id: statistics-api
  method: GET
  path: /balance-sheet-of-other-financial-intermediaries-by-geography-quarter
  description: Balance sheet of Other Financial Intermediaries, by geography (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getfinancialauxiliariesbalancesheetbygeographyquarter
  source_id: statistics-api
  method: GET
  path: /financial-auxiliaries-balance-sheet-by-geography-quarter
  description: Financial Auxiliaries balance sheet, by geography (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getloansbytypeandsecuritisationtypequarter
  source_id: statistics-api
  method: GET
  path: /loans-by-type-and-securitisation-type-quarter
  description: Loans by type and securitisation type (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getotherfinancialintermediariesbalancesheet
  source_id: statistics-api
  method: GET
  path: /other-financial-intermediaries-balance-sheet
  description: Other Financial Intermediaries balance sheet
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getsecuritisationvehiclesbalancesheet
  source_id: statistics-api
  method: GET
  path: /securitisation-vehicles-balance-sheet
  description: Securitisation Vehicles balance sheet
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getbalanceofpaymentsquarter
  source_id: statistics-api
  method: GET
  path: /balance-of-payments-quarter
  description: Balance of payments (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getbalanceofpaymentsyear
  source_id: statistics-api
  method: GET
  path: /balance-of-payments-year
  description: Balance of payments (Year)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getinfrastructureofdomesticretailpaymentsunits
  source_id: statistics-api
  method: GET
  path: /infrastructure-of-domestic-retail-payments-units
  description: Infrastructure of domestic retail payments (units)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getgeographicaldistributionofinwardandoutwardfdiincome
  source_id: statistics-api
  method: GET
  path: /geographical-distribution-of-inward-and-outward-fdi-income
  description: Geographical distribution of inward and outward FDI income
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getresidentialmortgageloansprovidedtohouseholdsbysector
  source_id: statistics-api
  method: GET
  path: /residential-mortgage-loans-provided-to-households-by-sector
  description: Residential mortgage loans provided to households by sector
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getpensionfundsbalancesheet
  source_id: statistics-api
  method: GET
  path: /pension-funds-balance-sheet
  description: Pension funds balance sheet
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getnumberandvalueofretailpaymenttransactions
  source_id: statistics-api
  method: GET
  path: /number-and-value-of-retail-payment-transactions
  description: Number and value of retail payment transactions
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getdutchgovernmentpaperholdings
  source_id: statistics-api
  method: GET
  path: /dutch-government-paper-holdings
  description: Dutch government paper holdings
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getinsurancecorporationsbalancesheetquarter
  source_id: statistics-api
  method: GET
  path: /insurance-corporations-balance-sheet-quarter
  description: Insurance corporations balance sheet (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getsummarybalancesheetofinsurancecorporationsbytypequarter
  source_id: statistics-api
  method: GET
  path: /summary-balance-sheet-of-insurance-corporations-by-type-quarter
  description: Summary balance sheet of insurance corporations by type (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getinsurancecorporationassetsandliabilitiesbydomesticcountersectorquarter
  source_id: statistics-api
  method: GET
  path: /insurance-corporation-assets-and-liabilities-by-domestic-countersector-quarter
  description: Insurance corporation assets and liabilities by domestic countersector
    (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getinsurancecorporationassetsandliabilitiesbygeographyquarter
  source_id: statistics-api
  method: GET
  path: /insurance-corporation-assets-and-liabilities-by-geography-quarter
  description: Insurance corporation assets and liabilities by geography (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getsummarybalancesheetofpensionfundsbytypequarter
  source_id: statistics-api
  method: GET
  path: /summary-balance-sheet-of-pension-funds-by-type-quarter
  description: Summary balance sheet of pension funds by type (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getpensionfundsassetsandliabilitiesbydomesticcountersectorquarter
  source_id: statistics-api
  method: GET
  path: /pension-funds-assets-and-liabilities-by-domestic-countersector-quarter
  description: Pension funds assets and liabilities by domestic countersector (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getpensionfundsassetsandliabilitiesbygeographicalareaquarter
  source_id: statistics-api
  method: GET
  path: /pension-funds-assets-and-liabilities-by-geographical-area-quarter
  description: Pension funds assets and liabilities by geographical area (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getdutchsecuritiesholdingsbysectoroftheholderbytypeofsecurity
  source_id: statistics-api
  method: GET
  path: /dutch-securities-holdings-by-sector-of-the-holder-by-type-of-security
  description: Dutch securities holdings by sector of the holder, by type of security
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getdutchsecuritiesholdingsbysectoroftheholderbygeography
  source_id: statistics-api
  method: GET
  path: /dutch-securities-holdings-by-sector-of-the-holder-by-geography
  description: Dutch securities holdings by sector of the holder, by geography
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getdutchdebtsecuritiesholdingsbysectoroftheholder
  source_id: statistics-api
  method: GET
  path: /dutch-debt-securities-holdings-by-sector-of-the-holder
  description: Dutch debt securities holdings by sector of the holder
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getdutchlistedsharesholdingsbysectoroftheholder
  source_id: statistics-api
  method: GET
  path: /dutch-listed-shares-holdings-by-sector-of-the-holder
  description: Dutch listed shares holdings by sector of the holder
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getdutchfundunitholdingsbysectoroftheholder
  source_id: statistics-api
  method: GET
  path: /dutch-fund-unit-holdings-by-sector-of-the-holder
  description: Dutch fund unit holdings by sector of the holder
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getnetexternalassetsquarter
  source_id: statistics-api
  method: GET
  path: /net-external-assets-quarter
  description: Net external assets (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getnetexternalassetsyear
  source_id: statistics-api
  method: GET
  path: /net-external-assets-year
  description: Net external assets (Year)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getmacroeconomicscoreboardquarter
  source_id: statistics-api
  method: GET
  path: /macroeconomic-scoreboard-quarter
  description: Macroeconomic scoreboard (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getmacroeconomicscoreboardyear
  source_id: statistics-api
  method: GET
  path: /macroeconomic-scoreboard-year
  description: Macroeconomic scoreboard (Year)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getnetdepositsininvestmentfundsbysectoroftheholderquarter
  source_id: statistics-api
  method: GET
  path: /net-deposits-in-investment-funds-by-sector-of-the-holder-quarter
  description: Net deposits in investment funds by sector of the holder (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getinsurerscashflowstatementquarter
  source_id: statistics-api
  method: GET
  path: /insurers-cash-flow-statement-quarter
  description: Insurers cash flow statement (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getpensionfundscashflowstatementquarter
  source_id: statistics-api
  method: GET
  path: /pension-funds-cash-flow-statement-quarter
  description: Pension funds cash flow statement (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getpensionfundsbalancesheetincludinglookthroughparticipationindutchinvestmen
  source_id: statistics-api
  method: GET
  path: /pension-funds-balance-sheet-including-look-through-participation-in-dutch-investment-funds-quarter
  description: Pension funds’ balance sheet including look through participation in
    Dutch investment funds (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getresidentialmortgagesprovidedbydutchinstitutionalinvestorstodutchhousehold
  source_id: statistics-api
  method: GET
  path: /residential-mortgages-provided-by-dutch-institutional-investors-to-dutch-households-quarter
  description: Residential mortgages provided by Dutch institutional investors to
    Dutch households (Quarter)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getnumberandvalueofpaymenttransactionshalfyear
  source_id: statistics-api
  method: GET
  path: /number-and-value-of-payment-transactions-half-year
  description: Number and value of payment transactions (Half-year)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
- id: getinfrastructuredomesticpaymentsinunitshalfyear
  source_id: statistics-api
  method: GET
  path: /infrastructure-domestic-payments-in-units-half-year
  description: Infrastructure domestic payments in units (Half-year)
  parameters:
    query:
    - name: page
      type: integer
      required: false
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
    - name: pageSize
      type: integer
      required: false
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
    - name: sort
      type: string
      required: false
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
    - name: sortDirection
      type: string
      required: false
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
