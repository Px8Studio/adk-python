sources:
  statistics_api:
    kind: http
    baseUrl: https://api.dnb.nl/statisticsdata/v2024100101
    headers:
      Ocp-Apim-Subscription-Key: ${DNB_SUBSCRIPTION_KEY_DEV}
    timeout: 30s
tools:
  dnb_statistics_exchange_rates_of_the_euro_and_gold_price_day:
    kind: http
    source: statistics_api
    method: GET
    path: /exchange-rates-of-the-euro-and-gold-price-day
    description: Exchange rates of the euro and gold price (Day)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_market_interest_rates_day:
    kind: http
    source: statistics_api
    method: GET
    path: /market-interest-rates-day
    description: Market interest rates (Day)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_market_interest_rates_month:
    kind: http
    source: statistics_api
    method: GET
    path: /market-interest-rates-month
    description: Market interest rates (Month)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_the_25_largest_investments_in_individual_listed_shares:
    kind: http
    source: statistics_api
    method: GET
    path: /the-25-largest-investments-in-individual-listed-shares
    description: The ‘25 largest’ investments in individual listed shares
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_dutch_household_savings_month:
    kind: http
    source: statistics_api
    method: GET
    path: /dutch-household-savings-month
    description: Dutch household savings (Month)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_mfi_household_deposits_and_loans_interest_rates_month:
    kind: http
    source: statistics_api
    method: GET
    path: /mfi-household-deposits-and-loans-interest-rates-month
    description: MFI household deposits and loans, interest rates (Month)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_statutory_interest_rate_half_year:
    kind: http
    source: statistics_api
    method: GET
    path: /statutory-interest-rate-half-year
    description: Statutory interest rate (Half-year)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_exchange_rates_of_the_euro_and_gold_price_month:
    kind: http
    source: statistics_api
    method: GET
    path: /exchange-rates-of-the-euro-and-gold-price-month
    description: Exchange rates of the euro and gold price (Month)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_balance_sheet_of_de_nederlandsche_bank_month_non_break_adjusted:
    kind: http
    source: statistics_api
    method: GET
    path: /balance-sheet-of-de-nederlandsche-bank-month-non-break-adjusted
    description: Balance sheet of De Nederlandsche Bank (Month, non-break adjusted)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_individual_pension_fund_data_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /individual-pension-fund-data-quarter
    description: Individual pension fund data (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_key_indicators_monetary_statistics_month:
    kind: http
    source: statistics_api
    method: GET
    path: /key-indicators-monetary-statistics-month
    description: Key indicators monetary statistics (Month)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_holding_of_bonds_by_type_of_sustainability_characteristic_by_sector:
    kind: http
    source: statistics_api
    method: GET
    path: /holding-of-bonds-by-type-of-sustainability-characteristic-by-sector
    description: Holding of bonds by type of sustainability characteristic by sector
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_holding_of_green_bonds_with_external_assurance_by_sector:
    kind: http
    source: statistics_api
    method: GET
    path: /holding-of-green-bonds-with-external-assurance-by-sector
    description: 'Holding of green bonds with external assurance by sector '
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_issuance_of_bonds_by_type_of_sustainability_characteristic_by_sector:
    kind: http
    source: statistics_api
    method: GET
    path: /issuance-of-bonds-by-type-of-sustainability-characteristic-by-sector
    description: Issuance of bonds by type of sustainability characteristic by sector
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_issuance_of_green_bonds_with_external_assurance_by_sector:
    kind: http
    source: statistics_api
    method: GET
    path: /issuance-of-green-bonds-with-external-assurance-by-sector
    description: Issuance of green bonds with external assurance by sector
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_investments_by_dutch_households_in_securities_by_instrument_category:
    kind: http
    source: statistics_api
    method: GET
    path: /investments-by-dutch-households-in-securities-by-instrument-category
    description: Investments by Dutch households in securities by instrument category
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_investments_by_dutch_households_in_individual_aex_shares:
    kind: http
    source: statistics_api
    method: GET
    path: /investments-by-dutch-households-in-individual-aex-shares
    description: Investments by Dutch households in individual AEX shares
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_the_25_largest_investments_by_dutch_households_in_individual_investment_funds:
    kind: http
    source: statistics_api
    method: GET
    path: /the-25-largest-investments-by-dutch-households-in-individual-investment-funds
    description: The ‘25 largest’ investments by Dutch households in individual investment
      funds
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_exchange_rates_of_the_euro_and_gold_price_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /exchange-rates-of-the-euro-and-gold-price-quarter
    description: Exchange rates of the euro and gold price (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_exchange_rates_of_the_euro_and_gold_price_year_end_of_period:
    kind: http
    source: statistics_api
    method: GET
    path: /exchange-rates-of-the-euro-and-gold-price-year-end-of-period
    description: Exchange rates of the euro and gold price (Year, end-of-period)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_exchange_rates_of_the_euro_and_gold_price_year_average:
    kind: http
    source: statistics_api
    method: GET
    path: /exchange-rates-of-the-euro-and-gold-price-year-average
    description: Exchange rates of the euro and gold price (Year, average)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_premiums_claims_and_costs_by_line_of_business_of_non_life_insurers_solvency_ii_year:
    kind: http
    source: statistics_api
    method: GET
    path: /premiums-claims-and-costs-by-line-of-business-of-non-life-insurers-solvency-ii-year
    description: Premiums, claims and costs by line of business of non-life insurers,
      Solvency II (Year)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_premiums_claims_and_costs_by_line_of_business_of_life_insurers_solvency_ii_year:
    kind: http
    source: statistics_api
    method: GET
    path: /premiums-claims-and-costs-by-line-of-business-of-life-insurers-solvency-ii-year
    description: Premiums, claims and costs by line of business of life insurers,
      Solvency II (Year)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_premiums_claims_and_costs_by_line_of_business_of_life_insurers_solvency_ii_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /premiums-claims-and-costs-by-line-of-business-of-life-insurers-solvency-ii-quarter
    description: Premiums, claims and costs by line of business of life insurers,
      Solvency II (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_premiums_claims_and_costs_by_line_of_business_of_non_life_insurers_solvency_ii_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /premiums-claims-and-costs-by-line-of-business-of-non-life-insurers-solvency-ii-quarter
    description: Premiums, claims and costs by line of business of non-life insurers,
      Solvency II (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_european_central_bank_interest_rates:
    kind: http
    source: statistics_api
    method: GET
    path: /european-central-bank-interest-rates
    description: European Central Bank interest rates
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_financial_data_of_individual_banks_half_year:
    kind: http
    source: statistics_api
    method: GET
    path: /financial-data-of-individual-banks-half-year
    description: Financial data of individual banks (Half-year)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_nominal_irts_for_pension_funds_zero_coupon:
    kind: http
    source: statistics_api
    method: GET
    path: /nominal-irts-for-pension-funds-zero-coupon
    description: Nominal IRTS for pension funds (zero coupon)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_balance_sheet_of_dutch_based_mfis_not_including_dnb_month:
    kind: http
    source: statistics_api
    method: GET
    path: /balance-sheet-of-dutch-based-mfis-not-including-dnb-month
    description: Balance sheet of Dutch-based MFIs (not including DNB) (Month)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_securities_and_derivatives_traders_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /securities-and-derivatives-traders-balance-sheet
    description: Securities and Derivatives Traders balance sheet
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_finance_companies_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /finance-companies-balance-sheet
    description: Finance Companies balance sheet
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_specialised_financial_institutions_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /specialised-financial-institutions-balance-sheet
    description: Specialised Financial Institutions balance sheet
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_financial_auxiliaries_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /financial-auxiliaries-balance-sheet
    description: Financial Auxiliaries balance sheet
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_head_offices_of_financial_auxiliaries_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /head-offices-of-financial-auxiliaries-balance-sheet
    description: Head Offices of Financial Auxiliaries balance sheet
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_other_financial_auxiliaries_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /other-financial-auxiliaries-balance-sheet
    description: Other Financial Auxiliaries balance sheet
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_balance_sheet_of_other_financial_intermediaries_by_geography_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /balance-sheet-of-other-financial-intermediaries-by-geography-quarter
    description: Balance sheet of Other Financial Intermediaries, by geography (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_financial_auxiliaries_balance_sheet_by_geography_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /financial-auxiliaries-balance-sheet-by-geography-quarter
    description: Financial Auxiliaries balance sheet, by geography (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_loans_by_type_and_securitisation_type_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /loans-by-type-and-securitisation-type-quarter
    description: Loans by type and securitisation type (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_other_financial_intermediaries_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /other-financial-intermediaries-balance-sheet
    description: Other Financial Intermediaries balance sheet
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_securitisation_vehicles_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /securitisation-vehicles-balance-sheet
    description: Securitisation Vehicles balance sheet
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_balance_of_payments_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /balance-of-payments-quarter
    description: Balance of payments (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_balance_of_payments_year:
    kind: http
    source: statistics_api
    method: GET
    path: /balance-of-payments-year
    description: Balance of payments (Year)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_infrastructure_of_domestic_retail_payments_units:
    kind: http
    source: statistics_api
    method: GET
    path: /infrastructure-of-domestic-retail-payments-units
    description: Infrastructure of domestic retail payments (units)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_geographical_distribution_of_inward_and_outward_fdi_income:
    kind: http
    source: statistics_api
    method: GET
    path: /geographical-distribution-of-inward-and-outward-fdi-income
    description: Geographical distribution of inward and outward FDI income
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_residential_mortgage_loans_provided_to_households_by_sector:
    kind: http
    source: statistics_api
    method: GET
    path: /residential-mortgage-loans-provided-to-households-by-sector
    description: Residential mortgage loans provided to households by sector
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_pension_funds_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /pension-funds-balance-sheet
    description: Pension funds balance sheet
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_number_and_value_of_retail_payment_transactions:
    kind: http
    source: statistics_api
    method: GET
    path: /number-and-value-of-retail-payment-transactions
    description: Number and value of retail payment transactions
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_dutch_government_paper_holdings:
    kind: http
    source: statistics_api
    method: GET
    path: /dutch-government-paper-holdings
    description: Dutch government paper holdings
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_insurance_corporations_balance_sheet_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /insurance-corporations-balance-sheet-quarter
    description: Insurance corporations balance sheet (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_summary_balance_sheet_of_insurance_corporations_by_type_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /summary-balance-sheet-of-insurance-corporations-by-type-quarter
    description: Summary balance sheet of insurance corporations by type (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_insurance_corporation_assets_and_liabilities_by_domestic_countersector_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /insurance-corporation-assets-and-liabilities-by-domestic-countersector-quarter
    description: Insurance corporation assets and liabilities by domestic countersector
      (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_insurance_corporation_assets_and_liabilities_by_geography_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /insurance-corporation-assets-and-liabilities-by-geography-quarter
    description: Insurance corporation assets and liabilities by geography (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_summary_balance_sheet_of_pension_funds_by_type_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /summary-balance-sheet-of-pension-funds-by-type-quarter
    description: Summary balance sheet of pension funds by type (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_pension_funds_assets_and_liabilities_by_domestic_countersector_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /pension-funds-assets-and-liabilities-by-domestic-countersector-quarter
    description: Pension funds assets and liabilities by domestic countersector (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_pension_funds_assets_and_liabilities_by_geographical_area_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /pension-funds-assets-and-liabilities-by-geographical-area-quarter
    description: Pension funds assets and liabilities by geographical area (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_dutch_securities_holdings_by_sector_of_the_holder_by_type_of_security:
    kind: http
    source: statistics_api
    method: GET
    path: /dutch-securities-holdings-by-sector-of-the-holder-by-type-of-security
    description: Dutch securities holdings by sector of the holder, by type of security
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_dutch_securities_holdings_by_sector_of_the_holder_by_geography:
    kind: http
    source: statistics_api
    method: GET
    path: /dutch-securities-holdings-by-sector-of-the-holder-by-geography
    description: Dutch securities holdings by sector of the holder, by geography
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_dutch_debt_securities_holdings_by_sector_of_the_holder:
    kind: http
    source: statistics_api
    method: GET
    path: /dutch-debt-securities-holdings-by-sector-of-the-holder
    description: Dutch debt securities holdings by sector of the holder
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_dutch_listed_shares_holdings_by_sector_of_the_holder:
    kind: http
    source: statistics_api
    method: GET
    path: /dutch-listed-shares-holdings-by-sector-of-the-holder
    description: Dutch listed shares holdings by sector of the holder
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_dutch_fund_unit_holdings_by_sector_of_the_holder:
    kind: http
    source: statistics_api
    method: GET
    path: /dutch-fund-unit-holdings-by-sector-of-the-holder
    description: Dutch fund unit holdings by sector of the holder
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_net_external_assets_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /net-external-assets-quarter
    description: Net external assets (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_net_external_assets_year:
    kind: http
    source: statistics_api
    method: GET
    path: /net-external-assets-year
    description: Net external assets (Year)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_macroeconomic_scoreboard_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /macroeconomic-scoreboard-quarter
    description: Macroeconomic scoreboard (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_macroeconomic_scoreboard_year:
    kind: http
    source: statistics_api
    method: GET
    path: /macroeconomic-scoreboard-year
    description: Macroeconomic scoreboard (Year)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_net_deposits_in_investment_funds_by_sector_of_the_holder_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /net-deposits-in-investment-funds-by-sector-of-the-holder-quarter
    description: Net deposits in investment funds by sector of the holder (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_insurers_cash_flow_statement_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /insurers-cash-flow-statement-quarter
    description: Insurers cash flow statement (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_pension_funds_cash_flow_statement_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /pension-funds-cash-flow-statement-quarter
    description: Pension funds cash flow statement (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_pension_funds_balance_sheet_including_look_through_participation_in_dutch_investment_funds_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /pension-funds-balance-sheet-including-look-through-participation-in-dutch-investment-funds-quarter
    description: Pension funds’ balance sheet including look through participation
      in Dutch investment funds (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_residential_mortgages_provided_by_dutch_institutional_investors_to_dutch_households_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /residential-mortgages-provided-by-dutch-institutional-investors-to-dutch-households-quarter
    description: Residential mortgages provided by Dutch institutional investors to
      Dutch households (Quarter)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_number_and_value_of_payment_transactions_half_year:
    kind: http
    source: statistics_api
    method: GET
    path: /number-and-value-of-payment-transactions-half-year
    description: Number and value of payment transactions (Half-year)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_infrastructure_domestic_payments_in_units_half_year:
    kind: http
    source: statistics_api
    method: GET
    path: /infrastructure-domestic-payments-in-units-half-year
    description: Infrastructure domestic payments in units (Half-year)
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
toolsets:
  dnb_statistics_tools:
  - dnb_statistics_exchange_rates_of_the_euro_and_gold_price_day
  - dnb_statistics_market_interest_rates_day
  - dnb_statistics_market_interest_rates_month
  - dnb_statistics_the_25_largest_investments_in_individual_listed_shares
  - dnb_statistics_dutch_household_savings_month
  - dnb_statistics_mfi_household_deposits_and_loans_interest_rates_month
  - dnb_statistics_statutory_interest_rate_half_year
  - dnb_statistics_exchange_rates_of_the_euro_and_gold_price_month
  - dnb_statistics_balance_sheet_of_de_nederlandsche_bank_month_non_break_adjusted
  - dnb_statistics_individual_pension_fund_data_quarter
  - dnb_statistics_key_indicators_monetary_statistics_month
  - dnb_statistics_holding_of_bonds_by_type_of_sustainability_characteristic_by_sector
  - dnb_statistics_holding_of_green_bonds_with_external_assurance_by_sector
  - dnb_statistics_issuance_of_bonds_by_type_of_sustainability_characteristic_by_sector
  - dnb_statistics_issuance_of_green_bonds_with_external_assurance_by_sector
  - dnb_statistics_investments_by_dutch_households_in_securities_by_instrument_category
  - dnb_statistics_investments_by_dutch_households_in_individual_aex_shares
  - dnb_statistics_the_25_largest_investments_by_dutch_households_in_individual_investment_funds
  - dnb_statistics_exchange_rates_of_the_euro_and_gold_price_quarter
  - dnb_statistics_exchange_rates_of_the_euro_and_gold_price_year_end_of_period
  - dnb_statistics_exchange_rates_of_the_euro_and_gold_price_year_average
  - dnb_statistics_premiums_claims_and_costs_by_line_of_business_of_non_life_insurers_solvency_ii_year
  - dnb_statistics_premiums_claims_and_costs_by_line_of_business_of_life_insurers_solvency_ii_year
  - dnb_statistics_premiums_claims_and_costs_by_line_of_business_of_life_insurers_solvency_ii_quarter
  - dnb_statistics_premiums_claims_and_costs_by_line_of_business_of_non_life_insurers_solvency_ii_quarter
  - dnb_statistics_european_central_bank_interest_rates
  - dnb_statistics_financial_data_of_individual_banks_half_year
  - dnb_statistics_nominal_irts_for_pension_funds_zero_coupon
  - dnb_statistics_balance_sheet_of_dutch_based_mfis_not_including_dnb_month
  - dnb_statistics_securities_and_derivatives_traders_balance_sheet
  - dnb_statistics_finance_companies_balance_sheet
  - dnb_statistics_specialised_financial_institutions_balance_sheet
  - dnb_statistics_financial_auxiliaries_balance_sheet
  - dnb_statistics_head_offices_of_financial_auxiliaries_balance_sheet
  - dnb_statistics_other_financial_auxiliaries_balance_sheet
  - dnb_statistics_balance_sheet_of_other_financial_intermediaries_by_geography_quarter
  - dnb_statistics_financial_auxiliaries_balance_sheet_by_geography_quarter
  - dnb_statistics_loans_by_type_and_securitisation_type_quarter
  - dnb_statistics_other_financial_intermediaries_balance_sheet
  - dnb_statistics_securitisation_vehicles_balance_sheet
  - dnb_statistics_balance_of_payments_quarter
  - dnb_statistics_balance_of_payments_year
  - dnb_statistics_infrastructure_of_domestic_retail_payments_units
  - dnb_statistics_geographical_distribution_of_inward_and_outward_fdi_income
  - dnb_statistics_residential_mortgage_loans_provided_to_households_by_sector
  - dnb_statistics_pension_funds_balance_sheet
  - dnb_statistics_number_and_value_of_retail_payment_transactions
  - dnb_statistics_dutch_government_paper_holdings
  - dnb_statistics_insurance_corporations_balance_sheet_quarter
  - dnb_statistics_summary_balance_sheet_of_insurance_corporations_by_type_quarter
  - dnb_statistics_insurance_corporation_assets_and_liabilities_by_domestic_countersector_quarter
  - dnb_statistics_insurance_corporation_assets_and_liabilities_by_geography_quarter
  - dnb_statistics_summary_balance_sheet_of_pension_funds_by_type_quarter
  - dnb_statistics_pension_funds_assets_and_liabilities_by_domestic_countersector_quarter
  - dnb_statistics_pension_funds_assets_and_liabilities_by_geographical_area_quarter
  - dnb_statistics_dutch_securities_holdings_by_sector_of_the_holder_by_type_of_security
  - dnb_statistics_dutch_securities_holdings_by_sector_of_the_holder_by_geography
  - dnb_statistics_dutch_debt_securities_holdings_by_sector_of_the_holder
  - dnb_statistics_dutch_listed_shares_holdings_by_sector_of_the_holder
  - dnb_statistics_dutch_fund_unit_holdings_by_sector_of_the_holder
  - dnb_statistics_net_external_assets_quarter
  - dnb_statistics_net_external_assets_year
  - dnb_statistics_macroeconomic_scoreboard_quarter
  - dnb_statistics_macroeconomic_scoreboard_year
  - dnb_statistics_net_deposits_in_investment_funds_by_sector_of_the_holder_quarter
  - dnb_statistics_insurers_cash_flow_statement_quarter
  - dnb_statistics_pension_funds_cash_flow_statement_quarter
  - dnb_statistics_pension_funds_balance_sheet_including_look_through_participation_in_dutch_investment_funds_quarter
  - dnb_statistics_residential_mortgages_provided_by_dutch_institutional_investors_to_dutch_households_quarter
  - dnb_statistics_number_and_value_of_payment_transactions_half_year
  - dnb_statistics_infrastructure_domestic_payments_in_units_half_year
