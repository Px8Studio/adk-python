# ============================================================================
# Base Configuration
# GenAI Toolbox - Shared Settings
# ============================================================================
# This file is loaded first (00- prefix) and can contain:
# - Shared authentication services
# - Common toolsets
# - Base configurations
#
# Individual API configurations are in separate numbered files:
#   10-dnb-echo.generated.yaml
#   20-dnb-statistics.generated.yaml  
#   30-dnb-public-register.generated.yaml
# ============================================================================

# Shared toolsets (optional - define groups that span multiple APIs)
# Format: toolset_name is a list of tool IDs
#
# NOTE: Individual API toolsets are auto-generated in their respective files:
#   - dnb-echo-tools (in 10-dnb-echo.generated.yaml)
#   - dnb-statistics-tools (in 20-dnb-statistics.generated.yaml)  
#   - dnb-public-register-tools (in 30-dnb-public-register.generated.yaml)
#
# You can define cross-API toolsets here if needed, for example:
# toolsets:
#   dnb-essentials:
#     - retrieve-resource  # from echo
#     - getexchangeratesoftheeuroandgoldpriceday  # from statistics
#     - get-api-publicregister-registers  # from public-register
