sources:
  statistics_api:
    kind: http
    baseUrl: https://api.dnb.nl/statisticsdata/v2024100101
    headers:
      Ocp-Apim-Subscription-Key: ${DNB_SUBSCRIPTION_KEY_PROD}
    timeout: 30s
tools:
  dnb_statistics_exchange_rates_of_the_euro_and_gold_price_day:
    kind: http
    source: statistics_api
    method: GET
    path: /exchange-rates-of-the-euro-and-gold-price-day
    description: 'Exchange rates of the euro and gold price (Day)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_market_interest_rates_day:
    kind: http
    source: statistics_api
    method: GET
    path: /market-interest-rates-day
    description: 'Market interest rates (Day)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_market_interest_rates_month:
    kind: http
    source: statistics_api
    method: GET
    path: /market-interest-rates-month
    description: 'Market interest rates (Month)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_the_25_largest_investments_in_individual_c9c06213:
    kind: http
    source: statistics_api
    method: GET
    path: /the-25-largest-investments-in-individual-listed-shares
    description: 'The ‘25 largest’ investments in individual listed shares

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_dutch_household_savings_month:
    kind: http
    source: statistics_api
    method: GET
    path: /dutch-household-savings-month
    description: 'Dutch household savings (Month)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_mfi_household_deposits_and_loans_interes_df5bc163:
    kind: http
    source: statistics_api
    method: GET
    path: /mfi-household-deposits-and-loans-interest-rates-month
    description: 'MFI household deposits and loans, interest rates (Month)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_statutory_interest_rate_half_year:
    kind: http
    source: statistics_api
    method: GET
    path: /statutory-interest-rate-half-year
    description: 'Statutory interest rate (Half-year)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_exchange_rates_of_the_euro_and_gold_price_month:
    kind: http
    source: statistics_api
    method: GET
    path: /exchange-rates-of-the-euro-and-gold-price-month
    description: 'Exchange rates of the euro and gold price (Month)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_balance_sheet_of_de_nederlandsche_bank_m_879d7312:
    kind: http
    source: statistics_api
    method: GET
    path: /balance-sheet-of-de-nederlandsche-bank-month-non-break-adjusted
    description: 'Balance sheet of De Nederlandsche Bank (Month, non-break adjusted)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_individual_pension_fund_data_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /individual-pension-fund-data-quarter
    description: 'Individual pension fund data (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_key_indicators_monetary_statistics_month:
    kind: http
    source: statistics_api
    method: GET
    path: /key-indicators-monetary-statistics-month
    description: 'Key indicators monetary statistics (Month)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_holding_of_bonds_by_type_of_sustainabili_100ac5e2:
    kind: http
    source: statistics_api
    method: GET
    path: /holding-of-bonds-by-type-of-sustainability-characteristic-by-sector
    description: 'Holding of bonds by type of sustainability characteristic by sector

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_holding_of_green_bonds_with_external_ass_054b9c53:
    kind: http
    source: statistics_api
    method: GET
    path: /holding-of-green-bonds-with-external-assurance-by-sector
    description: "Holding of green bonds with external assurance by sector \nNote:\
      \ parameter names are case-sensitive. Send exact names: pageSize, sortDirection"
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_issuance_of_bonds_by_type_of_sustainabil_970312da:
    kind: http
    source: statistics_api
    method: GET
    path: /issuance-of-bonds-by-type-of-sustainability-characteristic-by-sector
    description: 'Issuance of bonds by type of sustainability characteristic by sector

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_issuance_of_green_bonds_with_external_as_81fa4fc5:
    kind: http
    source: statistics_api
    method: GET
    path: /issuance-of-green-bonds-with-external-assurance-by-sector
    description: 'Issuance of green bonds with external assurance by sector

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_investments_by_dutch_households_in_secur_812b219e:
    kind: http
    source: statistics_api
    method: GET
    path: /investments-by-dutch-households-in-securities-by-instrument-category
    description: 'Investments by Dutch households in securities by instrument category

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_investments_by_dutch_households_in_indiv_0483d380:
    kind: http
    source: statistics_api
    method: GET
    path: /investments-by-dutch-households-in-individual-aex-shares
    description: 'Investments by Dutch households in individual AEX shares

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_the_25_largest_investments_by_dutch_hous_5ae73d15:
    kind: http
    source: statistics_api
    method: GET
    path: /the-25-largest-investments-by-dutch-households-in-individual-investment-funds
    description: 'The ‘25 largest’ investments by Dutch households in individual investment
      funds

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_exchange_rates_of_the_euro_and_gold_price_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /exchange-rates-of-the-euro-and-gold-price-quarter
    description: 'Exchange rates of the euro and gold price (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_exchange_rates_of_the_euro_and_gold_pric_45d34277:
    kind: http
    source: statistics_api
    method: GET
    path: /exchange-rates-of-the-euro-and-gold-price-year-end-of-period
    description: 'Exchange rates of the euro and gold price (Year, end-of-period)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_exchange_rates_of_the_euro_and_gold_pric_d4e33d49:
    kind: http
    source: statistics_api
    method: GET
    path: /exchange-rates-of-the-euro-and-gold-price-year-average
    description: 'Exchange rates of the euro and gold price (Year, average)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_premiums_claims_and_costs_by_line_of_bus_7109a3eb:
    kind: http
    source: statistics_api
    method: GET
    path: /premiums-claims-and-costs-by-line-of-business-of-non-life-insurers-solvency-ii-year
    description: 'Premiums, claims and costs by line of business of non-life insurers,
      Solvency II (Year)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_premiums_claims_and_costs_by_line_of_bus_24381852:
    kind: http
    source: statistics_api
    method: GET
    path: /premiums-claims-and-costs-by-line-of-business-of-life-insurers-solvency-ii-year
    description: 'Premiums, claims and costs by line of business of life insurers,
      Solvency II (Year)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_premiums_claims_and_costs_by_line_of_bus_a928c74d:
    kind: http
    source: statistics_api
    method: GET
    path: /premiums-claims-and-costs-by-line-of-business-of-life-insurers-solvency-ii-quarter
    description: 'Premiums, claims and costs by line of business of life insurers,
      Solvency II (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_premiums_claims_and_costs_by_line_of_bus_d8eaa04e:
    kind: http
    source: statistics_api
    method: GET
    path: /premiums-claims-and-costs-by-line-of-business-of-non-life-insurers-solvency-ii-quarter
    description: 'Premiums, claims and costs by line of business of non-life insurers,
      Solvency II (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_european_central_bank_interest_rates:
    kind: http
    source: statistics_api
    method: GET
    path: /european-central-bank-interest-rates
    description: 'European Central Bank interest rates

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_financial_data_of_individual_banks_half_year:
    kind: http
    source: statistics_api
    method: GET
    path: /financial-data-of-individual-banks-half-year
    description: 'Financial data of individual banks (Half-year)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_nominal_irts_for_pension_funds_zero_coupon:
    kind: http
    source: statistics_api
    method: GET
    path: /nominal-irts-for-pension-funds-zero-coupon
    description: 'Nominal IRTS for pension funds (zero coupon)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_balance_sheet_of_dutch_based_mfis_not_in_55e0a824:
    kind: http
    source: statistics_api
    method: GET
    path: /balance-sheet-of-dutch-based-mfis-not-including-dnb-month
    description: 'Balance sheet of Dutch-based MFIs (not including DNB) (Month)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_securities_and_derivatives_traders_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /securities-and-derivatives-traders-balance-sheet
    description: 'Securities and Derivatives Traders balance sheet

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_finance_companies_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /finance-companies-balance-sheet
    description: 'Finance Companies balance sheet

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_specialised_financial_institutions_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /specialised-financial-institutions-balance-sheet
    description: 'Specialised Financial Institutions balance sheet

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_financial_auxiliaries_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /financial-auxiliaries-balance-sheet
    description: 'Financial Auxiliaries balance sheet

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_head_offices_of_financial_auxiliaries_ba_76ad7414:
    kind: http
    source: statistics_api
    method: GET
    path: /head-offices-of-financial-auxiliaries-balance-sheet
    description: 'Head Offices of Financial Auxiliaries balance sheet

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_other_financial_auxiliaries_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /other-financial-auxiliaries-balance-sheet
    description: 'Other Financial Auxiliaries balance sheet

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_balance_sheet_of_other_financial_interme_7820428a:
    kind: http
    source: statistics_api
    method: GET
    path: /balance-sheet-of-other-financial-intermediaries-by-geography-quarter
    description: 'Balance sheet of Other Financial Intermediaries, by geography (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_financial_auxiliaries_balance_sheet_by_g_770492e7:
    kind: http
    source: statistics_api
    method: GET
    path: /financial-auxiliaries-balance-sheet-by-geography-quarter
    description: 'Financial Auxiliaries balance sheet, by geography (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_loans_by_type_and_securitisation_type_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /loans-by-type-and-securitisation-type-quarter
    description: 'Loans by type and securitisation type (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_other_financial_intermediaries_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /other-financial-intermediaries-balance-sheet
    description: 'Other Financial Intermediaries balance sheet

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_securitisation_vehicles_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /securitisation-vehicles-balance-sheet
    description: 'Securitisation Vehicles balance sheet

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_balance_of_payments_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /balance-of-payments-quarter
    description: 'Balance of payments (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_balance_of_payments_year:
    kind: http
    source: statistics_api
    method: GET
    path: /balance-of-payments-year
    description: 'Balance of payments (Year)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_infrastructure_of_domestic_retail_payments_units:
    kind: http
    source: statistics_api
    method: GET
    path: /infrastructure-of-domestic-retail-payments-units
    description: 'Infrastructure of domestic retail payments (units)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_geographical_distribution_of_inward_and__9b00c36c:
    kind: http
    source: statistics_api
    method: GET
    path: /geographical-distribution-of-inward-and-outward-fdi-income
    description: 'Geographical distribution of inward and outward FDI income

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_residential_mortgage_loans_provided_to_h_73905e51:
    kind: http
    source: statistics_api
    method: GET
    path: /residential-mortgage-loans-provided-to-households-by-sector
    description: 'Residential mortgage loans provided to households by sector

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_pension_funds_balance_sheet:
    kind: http
    source: statistics_api
    method: GET
    path: /pension-funds-balance-sheet
    description: 'Pension funds balance sheet

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_number_and_value_of_retail_payment_transactions:
    kind: http
    source: statistics_api
    method: GET
    path: /number-and-value-of-retail-payment-transactions
    description: 'Number and value of retail payment transactions

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_dutch_government_paper_holdings:
    kind: http
    source: statistics_api
    method: GET
    path: /dutch-government-paper-holdings
    description: 'Dutch government paper holdings

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_insurance_corporations_balance_sheet_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /insurance-corporations-balance-sheet-quarter
    description: 'Insurance corporations balance sheet (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_summary_balance_sheet_of_insurance_corpo_6860e4a1:
    kind: http
    source: statistics_api
    method: GET
    path: /summary-balance-sheet-of-insurance-corporations-by-type-quarter
    description: 'Summary balance sheet of insurance corporations by type (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_insurance_corporation_assets_and_liabili_6804c98f:
    kind: http
    source: statistics_api
    method: GET
    path: /insurance-corporation-assets-and-liabilities-by-domestic-countersector-quarter
    description: 'Insurance corporation assets and liabilities by domestic countersector
      (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_insurance_corporation_assets_and_liabili_89079593:
    kind: http
    source: statistics_api
    method: GET
    path: /insurance-corporation-assets-and-liabilities-by-geography-quarter
    description: 'Insurance corporation assets and liabilities by geography (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_summary_balance_sheet_of_pension_funds_b_cc3cad26:
    kind: http
    source: statistics_api
    method: GET
    path: /summary-balance-sheet-of-pension-funds-by-type-quarter
    description: 'Summary balance sheet of pension funds by type (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_pension_funds_assets_and_liabilities_by__4c60bf9e:
    kind: http
    source: statistics_api
    method: GET
    path: /pension-funds-assets-and-liabilities-by-domestic-countersector-quarter
    description: 'Pension funds assets and liabilities by domestic countersector (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_pension_funds_assets_and_liabilities_by__7ee86dff:
    kind: http
    source: statistics_api
    method: GET
    path: /pension-funds-assets-and-liabilities-by-geographical-area-quarter
    description: 'Pension funds assets and liabilities by geographical area (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_dutch_securities_holdings_by_sector_of_t_033da665:
    kind: http
    source: statistics_api
    method: GET
    path: /dutch-securities-holdings-by-sector-of-the-holder-by-type-of-security
    description: 'Dutch securities holdings by sector of the holder, by type of security

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_dutch_securities_holdings_by_sector_of_t_74fed568:
    kind: http
    source: statistics_api
    method: GET
    path: /dutch-securities-holdings-by-sector-of-the-holder-by-geography
    description: 'Dutch securities holdings by sector of the holder, by geography

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_dutch_debt_securities_holdings_by_sector_e41ef893:
    kind: http
    source: statistics_api
    method: GET
    path: /dutch-debt-securities-holdings-by-sector-of-the-holder
    description: 'Dutch debt securities holdings by sector of the holder

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_dutch_listed_shares_holdings_by_sector_o_bc39d083:
    kind: http
    source: statistics_api
    method: GET
    path: /dutch-listed-shares-holdings-by-sector-of-the-holder
    description: 'Dutch listed shares holdings by sector of the holder

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_dutch_fund_unit_holdings_by_sector_of_the_holder:
    kind: http
    source: statistics_api
    method: GET
    path: /dutch-fund-unit-holdings-by-sector-of-the-holder
    description: 'Dutch fund unit holdings by sector of the holder

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_net_external_assets_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /net-external-assets-quarter
    description: 'Net external assets (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_net_external_assets_year:
    kind: http
    source: statistics_api
    method: GET
    path: /net-external-assets-year
    description: 'Net external assets (Year)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_macroeconomic_scoreboard_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /macroeconomic-scoreboard-quarter
    description: 'Macroeconomic scoreboard (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_macroeconomic_scoreboard_year:
    kind: http
    source: statistics_api
    method: GET
    path: /macroeconomic-scoreboard-year
    description: 'Macroeconomic scoreboard (Year)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_net_deposits_in_investment_funds_by_sect_e7dea267:
    kind: http
    source: statistics_api
    method: GET
    path: /net-deposits-in-investment-funds-by-sector-of-the-holder-quarter
    description: 'Net deposits in investment funds by sector of the holder (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_insurers_cash_flow_statement_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /insurers-cash-flow-statement-quarter
    description: 'Insurers cash flow statement (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_pension_funds_cash_flow_statement_quarter:
    kind: http
    source: statistics_api
    method: GET
    path: /pension-funds-cash-flow-statement-quarter
    description: 'Pension funds cash flow statement (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_pension_funds_balance_sheet_including_lo_ff387084:
    kind: http
    source: statistics_api
    method: GET
    path: /pension-funds-balance-sheet-including-look-through-participation-in-dutch-investment-funds-quarter
    description: 'Pension funds’ balance sheet including look through participation
      in Dutch investment funds (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_residential_mortgages_provided_by_dutch__ac55d37a:
    kind: http
    source: statistics_api
    method: GET
    path: /residential-mortgages-provided-by-dutch-institutional-investors-to-dutch-households-quarter
    description: 'Residential mortgages provided by Dutch institutional investors
      to Dutch households (Quarter)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_number_and_value_of_payment_transactions_ab6631af:
    kind: http
    source: statistics_api
    method: GET
    path: /number-and-value-of-payment-transactions-half-year
    description: 'Number and value of payment transactions (Half-year)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
  dnb_statistics_infrastructure_domestic_payments_in_unit_3fd74d5b:
    kind: http
    source: statistics_api
    method: GET
    path: /infrastructure-domestic-payments-in-units-half-year
    description: 'Infrastructure domestic payments in units (Half-year)

      Note: parameter names are case-sensitive. Send exact names: pageSize, sortDirection'
    queryParams:
    - name: page
      type: integer
      description: The page number. If the page size is 0, the requested page number
        should be 1 or left empty. This wil yield all records.
      required: false
    - name: pageSize
      type: integer
      description: The page size. If omitted, will default to 2000. A page size of
        0 will yield all records. Please be aware that a page size of 0, or a another
        large page size, might result in longer response times.
      required: false
    - name: sort
      type: string
      description: The default sort direction is ascending and may be omitted. A whitespace
        is used to separate the field name and the sort direction. The only allowed
        sort field name and sort directions are 'period desc' and 'period asc', no
        other field names are allowed. If a sort direction is specified, it overrides
        the direction specified in the 'sortDirection' parameter
      required: false
    - name: sortDirection
      type: string
      description: Specifies the default sort direction. If omitted, default to ascending
        (asc). The value of sortDirection is overridden if the sort direction is already
        specified in the 'sort' parameter.
      required: false
toolsets:
  dnb_statistics_tools:
  - dnb_statistics_exchange_rates_of_the_euro_and_gold_price_day
  - dnb_statistics_market_interest_rates_day
  - dnb_statistics_market_interest_rates_month
  - dnb_statistics_the_25_largest_investments_in_individual_c9c06213
  - dnb_statistics_dutch_household_savings_month
  - dnb_statistics_mfi_household_deposits_and_loans_interes_df5bc163
  - dnb_statistics_statutory_interest_rate_half_year
  - dnb_statistics_exchange_rates_of_the_euro_and_gold_price_month
  - dnb_statistics_balance_sheet_of_de_nederlandsche_bank_m_879d7312
  - dnb_statistics_individual_pension_fund_data_quarter
  - dnb_statistics_key_indicators_monetary_statistics_month
  - dnb_statistics_holding_of_bonds_by_type_of_sustainabili_100ac5e2
  - dnb_statistics_holding_of_green_bonds_with_external_ass_054b9c53
  - dnb_statistics_issuance_of_bonds_by_type_of_sustainabil_970312da
  - dnb_statistics_issuance_of_green_bonds_with_external_as_81fa4fc5
  - dnb_statistics_investments_by_dutch_households_in_secur_812b219e
  - dnb_statistics_investments_by_dutch_households_in_indiv_0483d380
  - dnb_statistics_the_25_largest_investments_by_dutch_hous_5ae73d15
  - dnb_statistics_exchange_rates_of_the_euro_and_gold_price_quarter
  - dnb_statistics_exchange_rates_of_the_euro_and_gold_pric_45d34277
  - dnb_statistics_exchange_rates_of_the_euro_and_gold_pric_d4e33d49
  - dnb_statistics_premiums_claims_and_costs_by_line_of_bus_7109a3eb
  - dnb_statistics_premiums_claims_and_costs_by_line_of_bus_24381852
  - dnb_statistics_premiums_claims_and_costs_by_line_of_bus_a928c74d
  - dnb_statistics_premiums_claims_and_costs_by_line_of_bus_d8eaa04e
  - dnb_statistics_european_central_bank_interest_rates
  - dnb_statistics_financial_data_of_individual_banks_half_year
  - dnb_statistics_nominal_irts_for_pension_funds_zero_coupon
  - dnb_statistics_balance_sheet_of_dutch_based_mfis_not_in_55e0a824
  - dnb_statistics_securities_and_derivatives_traders_balance_sheet
  - dnb_statistics_finance_companies_balance_sheet
  - dnb_statistics_specialised_financial_institutions_balance_sheet
  - dnb_statistics_financial_auxiliaries_balance_sheet
  - dnb_statistics_head_offices_of_financial_auxiliaries_ba_76ad7414
  - dnb_statistics_other_financial_auxiliaries_balance_sheet
  - dnb_statistics_balance_sheet_of_other_financial_interme_7820428a
  - dnb_statistics_financial_auxiliaries_balance_sheet_by_g_770492e7
  - dnb_statistics_loans_by_type_and_securitisation_type_quarter
  - dnb_statistics_other_financial_intermediaries_balance_sheet
  - dnb_statistics_securitisation_vehicles_balance_sheet
  - dnb_statistics_balance_of_payments_quarter
  - dnb_statistics_balance_of_payments_year
  - dnb_statistics_infrastructure_of_domestic_retail_payments_units
  - dnb_statistics_geographical_distribution_of_inward_and__9b00c36c
  - dnb_statistics_residential_mortgage_loans_provided_to_h_73905e51
  - dnb_statistics_pension_funds_balance_sheet
  - dnb_statistics_number_and_value_of_retail_payment_transactions
  - dnb_statistics_dutch_government_paper_holdings
  - dnb_statistics_insurance_corporations_balance_sheet_quarter
  - dnb_statistics_summary_balance_sheet_of_insurance_corpo_6860e4a1
  - dnb_statistics_insurance_corporation_assets_and_liabili_6804c98f
  - dnb_statistics_insurance_corporation_assets_and_liabili_89079593
  - dnb_statistics_summary_balance_sheet_of_pension_funds_b_cc3cad26
  - dnb_statistics_pension_funds_assets_and_liabilities_by__4c60bf9e
  - dnb_statistics_pension_funds_assets_and_liabilities_by__7ee86dff
  - dnb_statistics_dutch_securities_holdings_by_sector_of_t_033da665
  - dnb_statistics_dutch_securities_holdings_by_sector_of_t_74fed568
  - dnb_statistics_dutch_debt_securities_holdings_by_sector_e41ef893
  - dnb_statistics_dutch_listed_shares_holdings_by_sector_o_bc39d083
  - dnb_statistics_dutch_fund_unit_holdings_by_sector_of_the_holder
  - dnb_statistics_net_external_assets_quarter
  - dnb_statistics_net_external_assets_year
  - dnb_statistics_macroeconomic_scoreboard_quarter
  - dnb_statistics_macroeconomic_scoreboard_year
  - dnb_statistics_net_deposits_in_investment_funds_by_sect_e7dea267
  - dnb_statistics_insurers_cash_flow_statement_quarter
  - dnb_statistics_pension_funds_cash_flow_statement_quarter
  - dnb_statistics_pension_funds_balance_sheet_including_lo_ff387084
  - dnb_statistics_residential_mortgages_provided_by_dutch__ac55d37a
  - dnb_statistics_number_and_value_of_payment_transactions_ab6631af
  - dnb_statistics_infrastructure_domestic_payments_in_unit_3fd74d5b
