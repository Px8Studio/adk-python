## Orkhon local environment (do NOT commit secrets)

# Option A: Google AI API (simple API key)
# Set this to your Google AI Studio API key if you want to use the public Google AI API.
# Reference: https://ai.google.dev
#GOOGLE_API_KEY=

# Option B: Vertex AI (service account / gcloud auth)
# If you prefer Vertex AI, provide the project and region and ensure credentials are available.
# 1) With a service account key file:
#GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account.json
# 2) Or authenticate with gcloud ("gcloud auth application-default login") and omit the key file.

# Vertex AI runtime settings
#GOOGLE_CLOUD_PROJECT=your-gcp-project-id
#GOOGLE_CLOUD_LOCATION=us-central1

# ============================================================
# BigQuery Configuration
# ============================================================

# Required: GCP Project ID where BigQuery datasets are stored
# Usually the same as GOOGLE_CLOUD_PROJECT
BQ_DATA_PROJECT_ID=your-gcp-project-id

# Optional: Project for running queries (defaults to BQ_DATA_PROJECT_ID if not set)
BQ_COMPUTE_PROJECT_ID=your-gcp-project-id

# Note: Dataset IDs are configured in:
# backend/adk/agents/data_science/dnb_datasets_config.json

# ============================================================
# Vertex AI Code Interpreter Extension (Caching)
# ============================================================

# IMPORTANT: Use CODE_INTERPRETER_EXTENSION_NAME (not CODE_INTERPRETER_ID)
# This caches the Code Interpreter Extension to avoid ~20s recreation overhead.
# 
# First run: Leave this commented out. ADK will create an extension and log the ID.
# Subsequent runs: Copy the extension resource name from logs and uncomment:
#CODE_INTERPRETER_EXTENSION_NAME=projects/PROJECT_ID/locations/REGION/extensions/EXTENSION_ID
#
# Note: Extension region (us-central1) does NOT need to match BigQuery region (europe-west4).
# Code Interpreter can access BigQuery globally. Using us-central1 is recommended for:
# - Most stable Vertex AI region
# - Best feature availability
# - Lowest cost
# Cross-region latency is minimal (~50-100ms). Only change if you have strict data residency requirements.

# ============================================================
# Other Configuration
# ============================================================

# GenAI Toolbox server (leave default for local dev)
#TOOLBOX_SERVER_URL=http://localhost:5000

